{
  "name": "bbtg-news",
  "private": true,
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "test": "turbo run test",
    "test:e2e": "turbo run test:e2e",
    "lint": "turbo run lint",
    "deploy": "pnpm run build && cd infra && pnpm run deploy",
    "destroy": "cd infra && pnpm run destroy",
    "cdk:synth": "cd infra && pnpm synth",
    "cdk:bootstrap": "cd infra && npx cdk bootstrap aws://870456164660/eu-west-1 --profile personal -c environment=dev -c awsAccountId=870456164660 -c awsRegion=eu-west-1",
    "infra:up": "docker-compose up -d",
    "infra:down": "docker-compose down",
    "db:init": "tsx scripts/init-local.ts",
    "db:seed": "tsx scripts/seed.ts",
    "setup": "pnpm infra:up && sleep 3 && pnpm db:init && pnpm db:seed"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "turbo": "^2.4.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "packageManager": "pnpm@10.29.3"
}
